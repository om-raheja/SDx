import { test, expect } from '@playwright/test';

// Skipped - no student submissions in test environment
test.skip('comment flow', async ({ page }) => {
  test.skip(true, 'No submissions available');
});